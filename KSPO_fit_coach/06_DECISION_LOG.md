# 06. Decision Log

[CONFIRMED]
- CENTER/HOME 2트랙, measurement.source 구조
- 성인 홈 4종 / 어르신 홈 5종 배터리 및 미커버 요인 표시
- 악력 선택 모듈(equipment_verified)
- PAR-Q 전 배치, 공통 판정(1개 이상 '예' → 차단), 문항별 차등 판정 금지
- PAR-Q 7문항 원문(전신체상태설문지, 사용자 제공본) — 웹뷰 구현 프레임워크: Vue.js(v3, CDN 단일 HTML)로 전환
- PAR-Q 미통과 시 차단 범위(자가측정/자동처방/주간루틴)와 허용 범위(센터입력/영상열람/센터안내)
- 공식 인증등급(1~6등급) 비표시
- 리포트 구성요소(§10), 리포트 순서(§13)
- HOME 백분위 구간 표시 + §11 초기 구간 폭, 3m 표적 돌아오기는 `참고값` 배지
- BMI 점추정 + 별도 범주, 혈압은 정상/주의 스크리닝만, 허리둘레 기본 미측정
- 추천 파이프라인 단일화(기본구성 + 약점가중), 미측정 요인이 기본 루틴 제거 사유가 아님
- 자가보고 데이터는 가중치 전용, 백분위/점수로 미취급
- 장애유형별 자동처방 MVP 제외(서비스 이용 자체는 차단하지 않음)
- 다년도 규준(2022.01~2026.07), 절대악력 규준은 2024년 이후만
- 연령대 5세 단위(age_band)
- §34 고지 문구 고정

[CHANGEABLE]
- 색상/폰트/카드·버튼 디자인/아이콘/간격/애니메이션/전환 방식/차트 스타일/레이아웃
- 진행률 표시, 단계 분할, 카드형 UI, 스텝퍼, 모달, Bottom Sheet, 툴팁
- 문구 다듬기(의미·공식 인증 암시 변경 없이)

[BLOCKED] — §07 참조

[IMPLEMENTED]
- 14개 화면 전체 플로우(웹뷰 MVP), Mock 데이터 기반 리포트/추천
- PAR-Q 게이팅 로직, HOME 구간 백분위 표시, 미측정 요인 카드, 신뢰도 배지

[FIXED]
- 리포트 "또래 비교" 렌더링이 악력(grip_strength, RANGE_WIDTH 미정의 항목) 진입 시 `range(...)[0]` null 참조로 크래시하던 문제 수정. `comparisonRows` computed로 항목별 표시 모드(range/point/reference)를 사전에 계산해 템플릿에서 null 역참조가 발생하지 않도록 구조 변경. 악력은 §6 "정식 백분위 산출" 취지에 맞춰 point(단일 백분위) 모드로 표시.
- jsdom+Vue 마운트 테스트로 CENTER/HOME(성인·어르신)×악력유무×PAR-Q 차단 5개 경로 전수 재현·검증 완료.
